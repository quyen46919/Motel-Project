import React from 'react';
import PropTypes from 'prop-types';

PostNewItem.propTypes = {
    
};

function PostNewItem(props) {
    return (
        <div>
            THIS IS POST ITEM PAGE
        </div>
    );
}

export default PostNewItem;